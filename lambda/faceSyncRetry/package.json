{
  "name": "face-sync-retry",
  "version": "1.0.0",
  "description": "AWS Lambda: process FaceSyncQueue PENDING records and publish to MQTT",
  "main": "handler.js",
  "scripts": {
    "zip": "zip -r ../faceSyncRetry.zip . -x '*.git*' -x 'node_modules/*'"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "mqtt": "^5.0.0"
  }
}
